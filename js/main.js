// document.getElementsByClassName("are")[0].backgroundColor = "pink"

var newDi= document.createElement ('div')
newDi.className ="we"

var textHere = document.createTextNode ('im so tired')

newDi.appendChild(textHere)

// var adhere = document.getElementsByClassName ('')[0];
// adhere.appendChild(newDi)

// document.getElementById ('error').innerHTML = "<h1>testing</h1>"

var newHeader = document.createElement ('input')
var text = document.createTextNode ('testing')
newHeader.appendChild(text)
//<h1>testing</h1>

var are = document.getElementById ('are')
are.appendChild(newHeader)